---
import { t, type Locale } from '../../i18n/translations';

interface Props {
  lang?: Locale;
}

const { lang = 'cs' } = Astro.props;
const i18n = t(lang);
---

<div class="testimonial">
  <div class="testimonial__content">
    <p class="testimonial__text">&bdquo;{i18n.testimonialText}&ldquo;</p>
  </div>
  <div class="testimonial__author">
    <img
      src="/images/testimonial-avatar.jpg"
      alt="Tomáš Nedorost"
      width="60"
      height="60"
      class="testimonial__avatar"
    />
    <span class="testimonial__name">Tomáš Nedorost</span>
  </div>
</div>

<style>
  .testimonial {
    background: var(--color-white);
    border: 1px solid var(--color-border);
    padding: var(--space-md);
    border-radius: var(--radius);
  }

  .testimonial__text {
    font-style: italic;
    color: var(--color-gray);
    line-height: 1.7;
    margin-bottom: var(--space-sm);
  }

  .testimonial__author {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .testimonial__avatar {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    object-fit: cover;
  }

  .testimonial__name {
    font-weight: var(--fw-semibold);
    color: var(--color-accent);
    font-size: 0.9375rem;
  }
</style>
