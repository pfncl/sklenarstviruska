---
import Icon from '../ui/Icon.astro';
import { contact } from '../../data/contact';
---

<div class="contact-info">
  <p class="contact-info__intro">
    Stavte se u nás na prodejně! Budeme se vám osobně věnovat, poradíme a doporučíme ta nejlepší řešení. Pokud si hned nebudeme vědět rady, tak se za vás spojíme se specialisty v oboru.
  </p>

  <div class="contact-info__item">
    <Icon name="map-pin" size={20} />
    <p><strong>Adresa:</strong> {contact.address}</p>
  </div>

  <div class="contact-info__item">
    <Icon name="phone" size={20} />
    <p><strong>Telefon:</strong> <a href={`tel:${contact.phone.replace(/\s/g, '')}`}>{contact.phone}</a></p>
  </div>

  <div class="contact-info__item">
    <Icon name="mail" size={20} />
    <p><strong>Email:</strong> <a href={`mailto:${contact.email}`}>{contact.email}</a></p>
  </div>

  <div class="contact-info__item">
    <Icon name="clock" size={20} />
    <div>
      <p><strong>Provozní doba:</strong></p>
      {contact.hours.map((h) => (
        <p>{h.days}: {h.time}</p>
      ))}
    </div>
  </div>

  <div class="contact-info__item">
    <Icon name="facebook" size={20} />
    <p><strong>Facebook:</strong> <a href={contact.facebook} target="_blank" rel="noopener noreferrer">Sklenářství Ruská</a></p>
  </div>
</div>

<style>
  .contact-info {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .contact-info__intro {
    line-height: 1.7;
  }

  .contact-info__item {
    display: flex;
    gap: 0.75rem;
    align-items: flex-start;
    color: var(--color-accent);
  }

  .contact-info__item div {
    flex: 1;
  }

  .contact-info__item strong {
    color: var(--color-dark);
    font-size: 0.9375rem;
    margin-right: 0.25rem;
  }

  .contact-info__item p {
    margin: 0;
    font-size: 0.9375rem;
  }

  .contact-info__item a {
    color: var(--color-accent);
    text-decoration: none;
  }

  .contact-info__item a:hover {
    text-decoration: underline;
  }
</style>
